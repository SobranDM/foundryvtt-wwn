<p>{{localize "WWN.Focus.BonusSkillDialogBody"}}</p>
<label class="form-group">
  <span>{{localize "WWN.Focus.BonusSkillDialogLabel"}}</span>
  <select name="skill">
    {{#each skillOptions as |opt|}}
    <option value="{{opt.slug}}">{{opt.label}}</option>
    {{/each}}
  </select>
</label>
