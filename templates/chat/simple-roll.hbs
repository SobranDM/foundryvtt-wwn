{{#if breakdown}}
<div class="wwn-chat-meta wwn-chat-breakdown">
  <span class="wwn-chat-breakdown-label">{{localize "WWN.Roll.Formula"}}</span>
  <i class="fa-solid fa-circle-question wwn-chat-hint" data-tooltip="{{breakdown}}"
    title="{{breakdown}}" aria-label="{{localize 'WWN.Roll.BreakdownHint'}}"></i>
</div>
{{/if}}
