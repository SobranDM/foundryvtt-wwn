{{!-- Inline field hint: muted ? icon with Foundry data-tooltip. --}}
{{#if hintKey}}
<i class="fa-regular fa-circle-question wwn-field-hint" data-tooltip="{{localize hintKey}}"
  aria-label="{{localize hintKey}}"></i>
{{/if}}
